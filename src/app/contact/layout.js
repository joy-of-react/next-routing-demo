import React from 'react';

import './styles.css';

function ContactLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

export default ContactLayout;
